# Contributing to safirl-codev

Thanks for helping! Below is a quick guide to get you productive fast.

## Environment

```bash
# Unix/macOS
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pytest -q tests
